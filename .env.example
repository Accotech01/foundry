# Postgres connection string (Vercel Postgres, Supabase, Neon, Railway, etc all work)
DATABASE_URL="postgresql://user:password@host:5432/foundry_row?schema=public"

# Random long string used to sign session JWTs — generate with: openssl rand -base64 32
AUTH_SECRET="replace-with-a-long-random-string"
